# binance-crawler
Binance data crawler built with pure Python requests — no scrapers, no Selenium.  Supports multi-threaded crawling, date-range extraction, metadata generation, recurring scheduler (daily / X-minutes),  and live WebSocket updates to a React frontend.
